{
  "mcpServers": {
    "n8n": {
      "command": "npx",
      "args": ["-y", "n8n-mcp"],
      "env": {
        "MCP_MODE": "stdio",
        "LOG_LEVEL": "error",
        "DISABLE_CONSOLE_OUTPUT": "true",
        "N8N_API_URL": "${user_config.N8N_API_URL}",
        "N8N_API_KEY": "${user_config.N8N_API_KEY}"
      }
    }
  }
}
