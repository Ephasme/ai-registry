# SOPS config — encrypts secrets to the maintainer's age key so ciphertext
# is safe to commit to this public repo. Decryption requires the matching
# private key (~/.config/sops/age/keys.txt), which is NOT in the repo.
creation_rules:
  - path_regex: \.sops\.json$
    age: age1a83ee8atea5gq7mu7mjvjazwnyuy26aejtcfm37ffayepkzjkamstur93z
